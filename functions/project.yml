packages:
  - name: myApp
    actions:
      - name: hello
        runtime: 'nodejs:default'
        web: true
        environment:
          DB_URL: "${DB_URL}"
      - name: newRow
        runtime: 'nodejs:default'
        web: true
        environment:
          DB_URL: "${DB_URL}"
      - name: triggered
        runtime: 'nodejs:default'
        web: true
        environment:
          DB_URL: "${DB_URL}"
